const myName: string = "ReHan";
      //  For upperCase
let Upperized_myName  = myName.toUpperCase();
console.log(Upperized_myName);

      // For lowerCase
let Lowerized_myName = myName.toLowerCase();
console.log(Lowerized_myName)

      // For titleCase
let Capatalized_myName = myName.charAt(0).toUpperCase() + myName.slice(1).toLowerCase();
console.log(Capatalized_myName);