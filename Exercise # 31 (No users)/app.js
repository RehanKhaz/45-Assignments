var users = ["user1", "user2", "user3", "user4", "admin"];
if (users.length === 0) {
    console.log("We need the find some users!.");
}
users = [];
if (users.length === 0) {
    console.log("We need the find some users!.");
}
